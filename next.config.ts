import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, //개발 모드 하단의 Next.js 배지/아이콘 제거
  // buildActivity: false, //빌드 상태 표시 없애기 (세부 설정)
};

export default nextConfig;
