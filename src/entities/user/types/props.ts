export interface UserEmailProps {
  email: string;
  fontSize?: string;
}

export interface UserIdProps {
  id: string;
  fontSize?: string;
}

export interface UserNameProps {
  name: string;
  fontSize?: string;
}
